syntax = "proto3";

package filetransfer;

// Mensagem para enviar arquivo
message FileRequest {
  string filename = 1;
}

// Mensagem de resposta
message FileResponse {
  bytes content = 1;
}

// Serviço
service FileService {
  rpc GetFile(FileRequest) returns (FileResponse);
}
